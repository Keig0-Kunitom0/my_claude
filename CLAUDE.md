# my_claude

Claude Code のワークフロー系プラグインを配布するカタログ。**このリポジトリは公開されている。**

## 業務固有の情報を書かない（最重要）

ここに置くプラグインは**どのリポジトリでも動く汎用ツール**であり、配布物でもある。
特定のプロダクト・案件・組織に紐づく情報を書いてはならない。

**これはドキュメントだけの話ではない。コミットメッセージ・PR のタイトルと本文・
コードコメントのすべてが対象。** git 履歴に入ると除去に履歴書き換えと force push が要る。

### 書いてはならないもの

| 種別 | 例 |
|---|---|
| リポジトリ名・プロダクト名・サービス名 | 業務で使っている実在の名前 |
| 実在の issue / PR 番号 | 実際に存在する番号 |
| 実在のブランチ名 | 実際に切られたブランチ |
| 実在のファイルパス・クラス名・テーブル名 | 業務コードの構造が分かるもの |
| 業務ドメインの語彙 | 事業固有の概念・機能名・指標名 |
| 絶対パス | `/Users/<名前>/...` のようなローカルパス |
| 外部サービス名 | 連携先として実際に使っているもの |

### 例示に使う値

**架空の値だけを使う。実在の値を「たまたま手元にあったから」で書かない。**

| 種別 | 使う値 |
|---|---|
| issue / PR 番号 | `1234`、`5678` |
| ブランチ名 | `1234/add-retry-to-webhook`、`5678/fix-timeout-handling` |
| owner / repo | `{owner}/{repo}`、`foo/bar` |
| ファイルパス | `app/models/user.rb`、`lib/parser.rb` |
| 長いブランチ名（文字数制限の例示） | `1234/refactor-notification-delivery-pipeline` |

既存のプラグインが使っている値に揃えること。新しい架空値を増やすと、
どれが架空でどれが実在か読み手が判断できなくなる。

### 実測結果を引用するとき

挙動の裏付けとして実測値を書くのは**良いこと**。ただし**どこで測ったかを書かない。**

```
✗ <実在のリポジトリ名> 直近100本での実測: 数字始まり88本 / その他12本
✓ 実在リポジトリ直近100本での実測: 数字始まり88本 / その他12本
```

```
✗ 古いブランチには <実在のブランチ名> が存在するため
✓ 古いブランチには `_` 区切り・snake 形式のものが残っているため
```

**悪い例を書くときも実在の値を使わない。** `<実在のリポジトリ名>` のように
プレースホルダで示す。悪い例だからといって書いてよいことにはならない。

数値・比率・エラーコード・API のレスポンス構造は、固有名詞を伴わない限り書いてよい。
むしろ「実測で確認した」という根拠は残す価値がある。

### 禁止語リストを作らない

**実在の名前を列挙した denylist をこのリポジトリに置いてはならない。**
そのファイル自体が固有名詞を公開してしまい、本末転倒になる。

判断は「架空の値だけを使う」という積極的な規約で行う。迷ったら実在の値を書かない。

### 書く前に自問する

- この値は**手元の環境を知らない読み手にとって意味があるか**
- 実在の値である必要が本当にあるか。`1234` で説明が成立しないか
- **公開リポジトリに未来永劫残ってよい情報か**

---

## 作業する場所

**このクローン（`~/my_claude`）で作業する。**

`~/.claude/plugins/marketplaces/my-claude/` にもクローンが存在するが、そちらは
`/plugin marketplace add` が管理する配布物の取得先であり、**更新や再追加で再クローン
される可能性がある**。そこで作業すると未コミットの変更が消える。

---

## プラグインの構成

```
<plugin>/
├── .claude-plugin/plugin.json   name / description / version / author / homepage / repository
├── README.md                    使い方・設計判断
├── commands/*.md                スラッシュコマンド（plugin:command で名前空間が付く）
└── skills/<name>/SKILL.md       コマンドから「読んで従え」と指示される共通部品
```

`.claude-plugin/marketplace.json` の `plugins` 配列にエントリを追加すると配布対象になる。
**プラグイン側の `plugin.json` と `marketplace.json` の `version` は必ず揃える。**

### 手順を書き写さない

複数コマンドが同じ手順を踏む場合、手順は **skill 側に一本化**し、コマンド側は
差分となるパラメータだけを持つ。コマンドごとに手順をコピーするとコピペ増殖が起きる。

コマンド側には「手順は `<skill>` にある。読んで従うこと。ここに書き写してはならない」と明記する。

### コマンド名は完全修飾で書く

プラグインコマンドは `plugin:command` で名前空間が付く。他のプラグインのコマンドを
プロンプトに埋め込む場合、`/implement-issue` ではなく
`/implement-issue:implement-issue` と書く（修飾なしで解決される保証がない）。

### 特定リポジトリの規約をハードコードしない

ブランチの命名規則・ディレクトリ構成・デフォルトブランチ名などは、
**実行時に既存の状態から推測するか、引数で受け取る。**
読み取れない場合は `AskUserQuestion` でユーザーに聞き、推測で押し切らない。

---

## リリース

`marketplace.json` の source は `ref` を指定していないため、デフォルトブランチが参照される。
**main にマージした時点で公開される。**

作業はブランチを切って PR を出す。コミットメッセージは
`feat(<plugin>):` / `fix(<plugin>):` / `docs(<plugin>):` / `chore(release):` を使う。
